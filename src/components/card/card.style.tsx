import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  card: {
    marginTop: 16,
    paddingTop: 10,
    width: '100%',
    height: 150,
    backgroundColor: '#0000ff',
    color: '#FFFFFF',
    textAlign: 'center',
    fontSize: 14,
    fontWeight: 'bold',
  },
});
